import { SortByTypePipe } from './sort-by-type.pipe';

describe('SortByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
